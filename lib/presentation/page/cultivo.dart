import 'package:flutter/widgets.dart';

class CultivoPage extends StatelessWidget {
  const CultivoPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pagina de Cultivo', style: TextStyle(fontSize: 24)),
    );
  }
}
